Every time you make changes:

Edit your files in Visual Studio Code
Save (Ctrl+S)
Open Git Bash and navigate to your folder:

bash   cd ~/MyMathSite

Run these three commands:

bash   git add .
   git commit -m "describe what you changed"
   git push

Visit your live site and do a hard refresh (Ctrl+Shift+R) to see the updates